'use strict'

const {
  FUSION_ROOT
} = require('../environment')

const {
  readFile
} = require('../utils/promises')

const version = require('./version')

async function help () {
  version()
  console.log((await readFile(`${FUSION_ROOT}/bin/help.txt`)).toString())
}

module.exports = help
