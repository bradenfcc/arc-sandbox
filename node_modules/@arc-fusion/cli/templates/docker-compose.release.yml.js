'use strict'

module.exports = async ({ PROJECT_ROOT, REPO_NAME }) => {
  return {
    version: '3',
    services: {
      release: {
        image: `washpost/fusion-engine:\${FUSION_RELEASE:-latest}`,
        entrypoint: 'cat ./release.txt',
        command: '',
        container_name: 'fusion-release',
        network_mode: 'none'
      }
    }
  }
}
